pillow==9.4.0           # For image processing (PIL)
pytesseract==0.3.10     # For OCR
requests==2.28.1        # For HTTP requests (used in web scraping)
beautifulsoup4==4.11.1  # For parsing HTML content
openai==0.27.0          # OpenAI SDK for GPT-4 (Azure OpenAI compatibility)
streamlit==1.15.0       # For web app interface
